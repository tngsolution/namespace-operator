apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "namespace-operator.fullname" . }}-test-connection"
  labels:
    {{- include "namespace-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "namespace-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
